# Test Pull Request - About Page

This PR adds a simple About page component to demonstrate the pull request workflow.

## Changes Made

- Created a new React component `About.tsx` in `frontend/src/components/`
- The component displays basic information about the project
- Uses inline styling for simplicity
- Includes a features section and footer with current date

## Component Location

`frontend/src/components/About.tsx`

## Usage

The component can be imported and used like this:

```tsx
import About from './components/About';

// In your component or router
<About />
```

## Testing

This is a simple presentational component that can be visually verified by importing it into the app.
